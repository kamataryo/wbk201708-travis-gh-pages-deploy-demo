{
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-stylus": "^2.6.0"
  },
  "name": "wbk201708-travis-gh-pages-deploy-demo",
  "version": "1.0.0",
  "main": "gulpfile.js",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-stylus": "^2.6.0"
  },
  "scripts": {
    "build": "gulp",
    "pretest": "[ -d ./dist ] && rm -rf ./dist",
    "test": "npm run build && ls ./dist/style.css && echo 'test success!'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
